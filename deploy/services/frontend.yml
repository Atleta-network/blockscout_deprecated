version: '3.9'

services:
  frontend:
    image: atletanetwork/blockscout:frontend-latest
    pull_policy: never
    restart: always
    container_name: 'frontend'
    env_file:
      -  ../envs/common-frontend.env
